#!/usr/bin/env node

/**
 * Ponto de entrada principal do sistema
 * Inicia o servidor web Express para a interface de automação
 */

import { startServer } from './web-server.js';

// Iniciar servidor web
startServer();
